import HeaderComponent from './src/components/HeaderComponent.js'
import RouterComponent from './src/RouterComponent.js'

/* Appeler le composant de fonction Header ici me permet de ne plus avoir à le rappeler où que ce soit d'autre. */
HeaderComponent()
/* Initialise une première fois le routeur contenu dans le composant de fonction RouterComponent. */
RouterComponent()
